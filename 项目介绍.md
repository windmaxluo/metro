# 地铁换乘最少路径查询系统

## 一、项目简介

这是一个用C语言实现的地铁换乘查询系统。用户可以输入起点站和终点站，系统会自动计算出**换乘次数最少**的乘车路线，并输出详细的换乘指引（包括乘坐哪条线路、经过哪些站点、在哪里换乘）。

### 核心功能

- 支持多条地铁线路，每条线路有多个站点
- 自动识别线路之间的换乘站（公共站点）
- 使用BFS算法查找换乘次数最少的路径
- 输出完整的乘车路线，包括详细站点列表

---

## 二、核心设计思路

### 2.1 问题建模：把地铁网络抽象成图

地铁换乘问题的本质是**图论中的最短路径问题**，但有一个关键点：换乘次数最少 ≠ 经过的站点数最少。

**建模方式**：

- 把**每条地铁线路**看作图中的一个**节点**
- 如果两条线路有公共站点（即可以在该站换乘），就在这两个节点之间连一条**边**
- 边上记录换乘站的名称

这样，“换乘最少”就变成了：在**线路图**上找一条从起点线路到终点线路的**边数最少**的路径。


### 2.2 算法选择：BFS（广度优先搜索）

在线路图中，每条边的权重都是1（换乘一次），BFS天然适合求解无权图的最短路径。

**BFS保证**：第一次到达目标线路时，走过的边数（换乘次数）就是最小值。

### 2.3 路径输出：回溯法

BFS搜索过程中，记录每个线路的**前驱线路**和**换乘站名**。找到目标后，从终点线路往回追溯，得到完整的线路序列，再分段输出乘车指引。

---

## 三、数据结构设计

### 3.1 线路存储（Line结构体）

```c
typedef struct {
    int id;                                 // 线路编号 0,1,2...
    char name[NAME_MAX_LEN];                // 线路名称 "一号线"
    int station_count;                      // 该线路上的站点数量
    char stations[MAX_STATIONS_PER_LINE][NAME_MAX_LEN]; // 站点名数组
} Line;
```
### 3.2 换乘表(邻接矩阵)
```c
char transfer[MAX_LINES][MAX_LINES][NAME_MAX_LEN];
```
### 3.3 辅助数据结构
```c
visited[]	//int数组	BFS时标记线路是否已访问
prev_line[]	//int数组	记录每个线路的前驱线路
prev_station[][]	//char二维数组	记录从前驱换乘到本线的站名
queue[] + q_transfer[]	//int数组	队列，存储待处理的线路ID和换乘次数
```
## 四、核心函数说明

### 4.1line
```c
void line_init_data(void);
void printf_lines(int id);  //打印单条线路
int line_contains_station(int line_id, const char *station);   //线路是否包含某个站点  0无1有
int get_station_index(int line_id, const char *station);       //获取某个站点在线路的下标
void find_lines_by_station(const char *station, int line_ids[], int *count);// 查找包含某个站点的所有线路
```
### 4.2graph
```c
// 自动构建换乘图：遍历所有线路，找出公共站点，填入 transfer 表
void build_transfer_graph(void);
```
### 4.3path
```c
// 打印同一条线路内从 start 到 end 的路径（同一线路内）
void print_segment(int line_id, const char *start, const char *end);

// 查询最少换乘路径（主函数）
void find_path(const char *start, const char *end);
```
find_path流程
1. 找出起点和终点分别属于哪些线路
       ↓
2. 检查是否在同一条线路（换乘0次）→ 直接打印
       ↓
3. BFS搜索：
   - 起点线路入队，换乘次数=0
   - 循环取出队列中的线路
   - 检查是否包含终点 → 找到目标
   - 否则找所有能换乘的未访问线路，入队
       ↓
4. 回溯：从目标线路往回找前驱，得到线路序列
       ↓
5. 反转序列（因为回溯是从终点到起点）
       ↓
6. 分段打印：每段调用 print_segment

## 五、程序执行流程
1. 用户输入起点站A、终点站C
        ↓
2. find_path() 被调用
        ↓
3. find_lines_by_station() 查找起点/终点所在线路
        ↓
4. 检查同线路直达 → 是 → print_segment() → 结束
        ↓ 否
5. BFS在线路图上搜索
        ↓
6. 找到目标线路后回溯
        ↓
7. 分段输出：
  乘坐 X号线 从 A 到 B (A→...→B)
  在 B 换乘
  乘坐 Y号线 从 B 到 C (B→...→C)
  ...
## 六、示例
### 输入
起点站：蒙德城
终点站：望舒客栈
### 输出
换乘 1 次：
乘坐 一号线 从 蒙德城 到 天空岛(蒙德城->璃月港->稻妻城->天空岛)
在 天空岛 换乘
乘坐 二号线 从 天空岛 到 望舒客栈(天空岛->沉玉谷->琥牢山->望舒客栈)
## 七、总结
1. 项目特点	说明
2. 算法	BFS求无权图最短路径
3. 数据结构	邻接矩阵、队列、结构体
4. 代码规模	约400行，模块化设计
5. 核心难点	图建模、路径回溯、方向判断
6. 适用场景	地铁/公交换乘查询系统原型
